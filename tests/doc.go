// Package tests holds the Terratest suites for the module.
//
// The default suite applies the fixture in fixtures/instance against floci,
// a local AWS emulator. The suite behind the "aws" build tag deploys the
// examples to a real AWS account.
package tests
